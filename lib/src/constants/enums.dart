/// [LocalizationDefaultType] is used to define the default type of localization
enum LocalizationDefaultType {
  /// [device] is used to define the default type of localization as device
  device,

  /// [asDefined] is used to define the default type of localization as manual
  asDefined,
}
